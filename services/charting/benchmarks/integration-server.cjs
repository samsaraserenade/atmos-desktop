const fs=require('fs'),http=require('http'),path=require('path');const root=path.resolve(__dirname,'..');http.createServer((req,res)=>{const name=req.url==='/'?'benchmarks/canvas-integration.html':req.url.slice(1);if(!/^(?:[a-z-]+\.js|benchmarks\/canvas-integration\.html)$/.test(name)){res.writeHead(404);res.end();return}res.setHeader('Content-Type',name.endsWith('.js')?'text/javascript':'text/html');res.end(fs.readFileSync(path.join(root,name)))}).listen(8770,'127.0.0.1',()=>console.log('Integration preview ready'));
